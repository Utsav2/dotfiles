alias pdf='apvlv'
alias d='cd ~/Downloads'
alias p='cd ~/p'
alias t='cd /tmp'
alias open='xdg-open' 
alias q='exit'
alias :q='exit'
alias :wq='exit'
alias e='emacs . &'
alias studio='~/Downloads/android-studio/bin/studio.sh'
alias sys-update='yaourt -Syyu --noconfirm --aur'
alias igrep='grep -i'
alias g='git'

up() {
  for i in `seq 1 $1`; do
  cd ..
  done
}

md() {
  markdown "$@" | lynx -stdin
}

search() {
  google-chrome "google.com/search?q="$1 &>/dev/null
}


export PATH="/root/.gem/ruby/2.3.0/bin:$PATH"
export PATH="/home/utsav/.cabal/bin:$PATH"
export PATH="/home/utsav/Downloads/clion-1.2.4:$PATH"

export NVM_DIR="/home/utsav/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
